fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
python-dotenv==1.0.1
aiofiles==24.1.0
email-validator==2.2.0

# Optional: real OCR (falls back to a mock extractor if not installed)
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0

# Optional: real AI advisor (falls back to rule-based canned answers if no key is set)
openai==1.51.0

# PDF Generation
reportlab>=4.0.0

